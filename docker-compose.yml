services:
  altitude-dashboard:
    build: .
    container_name: altitude-dashboard
    ports:
      - "3131:3131"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

services:
  altitude-dashboard:
    image: ghcr.io/jpetree/altitude-dashboard:latest
    container_name: altitude-dashboard
    ports:
      - "3131:3131"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
